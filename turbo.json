{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": [".next/**", "web-app/**"]
    },
    "lint": {},
    "dev": {},
    "export": {
      "dependsOn": ["^build"],
      "outputs": ["web-app/**"]
    }
  }
}
