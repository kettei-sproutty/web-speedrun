{
  "name": "web-speedrun-root",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "apps/*"
  ],
  "packageManager": "pnpm@7.29.1",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "export": "turbo run export",
    "start": "turbo run start",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^1.8.3"
  }
}
